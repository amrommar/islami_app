import 'package:flutter/material.dart';

class Hadeth_Tab extends StatelessWidget {
  const Hadeth_Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
